



class Main{
    
    void print_float(float x) extern;
    void print_int(int i) extern;

    // Retorna 100
    int getX(){
        int i;
        i = 0;
        while(i<200){
            i += 1;
            // VER ESTO
            if(i==100){
                break;
            } else {
                continue;
            }
            continue;
            //codigo muerto
            i -= 1;
        }
        return i;
    }
    
    void main(){
        print_int( getX());
        return;
    }

}
