class Main {
    int global;
    
    int inc(int x) {
        return x+1;
    }

    

    void printInt(int x) extern;
    
    void main() {
        int y;
        printInt(0);
        y = inc(0);
        return;
    }
}