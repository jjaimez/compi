class Main {
    int global2;

    int global;
    int globalArray[5];
    
    int inc(int x) {
        return 1 + global;
    }

    int get_int() extern;

    void printInt(int x) extern;
    
    void main() {
        int y;
        y = inc(get_int());
        if (y == 1) {
            printInt(1);
        } else {
            printInt(0);
        }
    }
}